name: Create Issues
on:
  issue_comment:
    type: [created]

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - uses: paulovitin/create_issues@0.0.1
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
